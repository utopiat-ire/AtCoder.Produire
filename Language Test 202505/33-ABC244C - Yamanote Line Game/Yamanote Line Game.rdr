//https://github.com/utopiat-ire/AtCoder.Produire
※コンパイル
※コンソール
コンソールから受け取って【N:整数】とする
宣言済みという真偽値の配列(2*N+1)を作る
繰り返す
	【番号】に1から2*N+1まで繰り返す
		宣言済み(番号)が×なら
			宣言済みの(番号)番目は○
			コンソールへ「[番号]」を出力して改行する
			繰り返しから抜け出す
		そして
	そして
	コンソールから受け取って【返答:整数】とする
	返答が0なら繰り返しから抜け出す
	宣言済みの(返答)番目は○
そして
