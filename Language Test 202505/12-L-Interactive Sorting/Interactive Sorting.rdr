※コンソール
※コンパイル
コンソールから受け取って「 」で区切ってNQとする
N=NQ(1)
Q=NQ(2)
【S:配列】
S={}
0からN-1までiを増やしながら繰り返す
	(「A」の文字番号+i)を文字化してSに追加する
そして
Nが5なら
	Sを五ソートしてSとする
そうでなければ
	Sを1からNまでマージソートする
そして
「! [Sを「」で繋げたもの]」をコンソールに出力して改行する

【A】を【L:整数】から【R:整数】までマージソートする手順
	LがRなら返す
	【M:整数】=(L+(R-L)/2)を切り捨てる
	AをLからMまでマージソートする
	AをM+1からRまでマージソートする
	AをLからMでRまでマージする
終わり

【A】を【L:整数】から【M:整数】で【R:整数】までマージする手順
	【i:整数】=L
	【j:整数】=M+1
	【k:整数】=1
	【B】={}
	i<=Mかつj<=Rの間繰り返す
		A(i)がA(j)より、大きい値なら
			B(k)=A(j)
			jを増やす
		そうでなければ
			B(k)=A(i)
			iを増やす
		そして
		kを増やす
	そして

	i=M+1なら
		j<=Rの間繰り返す
			B(k)=A(j)
			jを増やす
			kを増やす
		そして
	そうでなければ
		i<=Mの間繰り返す
			B(k)=A(i)
			iを増やす
			kを増やす
		そして
	そして
	1からk-1までpを増やしながら繰り返す
		A(L+p-1)=B(p)
	そして
終わり


【A】を五ソートする手順
	A(1)がA(2)より、大きい値ならA(1)をA(2)と交換する
	A(3)がA(4)より、大きい値ならA(3)をA(4)と交換する
	A(1)がA(3)より、大きい値ならA={A(3),A(4),A(1),A(2),A(5)}
	A(3)がA(5)より、小さい値なら
		A(4)がA(5)より、大きい値ならA(4)をA(5)と交換する
		A(2)がA(4)より、小さい値なら
			A(2)がA(3)より小さい値なら
				A={A(1),A(2),A(3),A(4),A(5)}
			そうでなければ
				A={A(1),A(3),A(2),A(4),A(5)}
			そして
		そうでなければ
			A(2)がA(5)より小さい値なら
				A={A(1),A(3),A(4),A(2),A(5)}
			そうでなければ
				A={A(1),A(3),A(4),A(5),A(2)}
			そして
		そして
	そうでなければ
		A(1)がA(5)より、小さい値なら
			A(2)がA(3)より、小さい値なら
				A(2)がA(5)より、小さい値なら
					A={A(1),A(2),A(5),A(3),A(4)}
				そうでなければ
					A={A(1),A(5),A(2),A(3),A(4)}
				そして
			そうでなければ
				A(2)がA(4)より、小さい値なら
					A={A(1),A(5),A(3),A(2),A(4)}
				そうでなければ
					A={A(1),A(5),A(3),A(4),A(2)}
				そして
			そして
		そうでなければ
			A(2)がA(3)より、小さい値なら
				A={A(5),A(1),A(2),A(3),A(4)}
			そうでなければ
				A(2)がA(4)より、小さい値なら
					A={A(5),A(1),A(3),A(2),A(4)}
				そうでなければ
					A={A(5),A(1),A(3),A(4),A(2)}
				そして
			そして
		そして
	そして
	Aを返す
終わり

【X】が【Y】より、大きい値かどうかを判定する手順
	「? [X] [Y]」をコンソールに出力して改行する
	コンソールで聞いて答えとする
	(答えが「>」)を返す
終わり
【X】が【Y】より、小さい値かどうかを判定する手順
	[XがYより大きい値でない]を返す
終わり
